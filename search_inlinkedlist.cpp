#include<iostream>
using namespace std;
struct node{
    int info;
    node* next;
};
 node*search(node* head,int key){
     node*start=head;
     node*end=NULL;
      while(start!=end){
        node*mid=NULL;
         if(mid==NULL) return NULL;
         if(mid>data==key){
            return mid;
         }
      }
 }